<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="theme-color" content="#17a2b8">
         
    <!--    STYLES 

        Note: Express already looks into public folder, so no further path
        specification required if using public/main.css 
        BUT you do have to include / for root (public). If not your partial
        modules will try to load from /route/main.css and that will throw
        a MIME error.
    -->
    <link rel="stylesheet" href="/main.css" type="text/css">
    
    <!--    SEO    -->
    <title>CataCamp</title>
</head>
<body>
    <nav class="navbar navbar-expand-md navbar-dark bg-dark mb-4 border-bottom border-info">
        <div class="container">
            <button class="navbar-toggler" type="button" data-toggle="collapse" 
                    data-target="#navbarToggleExternalContent" aria-expanded="false"
                    aria-controls="navbarToggleExternalContent" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <a class="navbar-brand text-uppercase mr-4" href="/" title="START">
                CataCamp
            </a>
            <div class="collapse navbar-collapse justify-content-between" id="navbarToggleExternalContent">
                <ul class="navbar-nav">
                    <li class="nav-item d-none">
                        <a class="nav-link home" href="/campgrounds" title="HOME">
                            <i class="fas fa-home"></i></a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <% if(!currentUser) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/login" title="LOGIN">
                            <i class="fas fa-sign-in-alt fa-lg pr-2"></i>Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/register" title="REGISTER"><i class="fas fa-user-plus fa-1x pr-2"></i>Sign Up</a>
                    </li>
                    <% } else { %>
                    <li class="nav-item">
                        <a  class="nav-link" href="/user/<%= currentUser._id %>">
                            <i class="far fa-user fa-md pr-2"></i>Logged in as <%= currentUser.username %>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a  class="nav-link" href="/user/<%= currentUser._id %>" title="PROFILE"><i class="far fa-id-card fa-1x pr-2"></i>Profile</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout" title="LOGOUT"><i class="fas fa-sign-out-alt fa-1x pr-2"></i>Logout</a>
                    </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
    <!-- FLASH MESSAGES -->
    <div class="container">
        <% if(error && error.length > 0) { %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>
        <% if(success && success.length > 0) { %>
            <div class="alert alert-success" role="alert">
                <%= success %>
            </div>
        <% } %>
    </div>

