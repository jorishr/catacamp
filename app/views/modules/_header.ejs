<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
         
    <!--    STYLES 

        Note: Express already looks into public folder, so no further path
        specification required if using public/main.css 
        BUT you do have to include / for root (public). If not your partial
        modules will try to load from /route/main.css and that will throw
        a MIME error.
    -->
<!--     <link rel="stylesheet" href="/bootstrap-reboot.css" type="text/css">   
    <link rel="stylesheet" href="/bootstrap.css" type="text/css"> -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="/main.css" type="text/css">  

    <script src="https://kit.fontawesome.com/49d7940b9d.js"></script>
    <!--    SEO    -->
    <title>CataCamp</title>
</head>
<body>
    <nav class="navbar navbar-expand-md navbar-dark bg-dark mb-4">
        <button class="navbar-toggler" type="button" data-toggle="collapse" 
                data-target="#navbarToggleExternalContent" aria-expanded="false"
                aria-controls="navbarToggleExternalContent" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <a class="navbar-brand" href="/">CataCamp</a>
        <div class="collapse navbar-collapse justify-content-between" id="navbarToggleExternalContent">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/campgrounds"><i class="fas fa-campground pr-2"></i>Home</a>
                </li>
            </ul>
            <ul class="navbar-nav">
                <% if(!currentUser) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/login"><i class="fas fa-sign-in-alt pr-2"></i>Login</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/register"><i class="fas fa-user-plus pr-2"></i>Sign Up</a>
                </li>
                <% } else { %>
                <li class="nav-item">
                    <a  class="nav-link" href="/user/<%= currentUser._id %>">
                        <i class="far fa-user pr-2"></i>Logged in as <%= currentUser.username %>
                    </a>
                </li>
                <li class="nav-item">
                    <a  class="nav-link" href="/user/<%= currentUser._id %>"><i class="far fa-id-card pr-2"></i>Profile</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt pr-2"></i>Logout</a>
                </li>
                <% } %>
            </ul>
        </div>
    </nav>
    <!-- FLASH MESSAGES -->
    <div class="container">
        <% if(error && error.length > 0) { %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>
        <% if(success && success.length > 0) { %>
            <div class="alert alert-success" role="alert">
                <%= success %>
            </div>
        <% } %>
    </div>

